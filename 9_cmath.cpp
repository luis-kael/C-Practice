#include <iostream>
#include <cmath>
// don't forget cmath
int main()
{
    double x = 3.8;
    double y = 4;
    double z;
    // z = std::max(x, y);
    // z = std::min(x, y);
    // z = pow(2, 4); power
    // z = sqrt(9);
    // z = abs(-3); absolute postive number( change to positive )
    // z = round(x);
    // z = ceil(x); highest
    // z = floor(x); lowest

    std::cout << z;

    return 0;
}